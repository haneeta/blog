{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "hexo clean && cd blog-en && hexo clean && hexo generate --silent && cd ../ && mkdir public && cp -r blog-en/public public/en && hexo generate --silent",
    "build:online": "cp -r node_modules blog-en && cd blog-en && hexo generate --silent && cd ../ && mkdir public && cp -r blog-en/public public/en && hexo generate --silent",
    "clean": "hexo clean",
    "server": "hexo clean && cd blog-en && hexo clean && hexo generate --silent && cd ../ && mkdir public && cp -r blog-en/public public/en && hexo s"
  },
  "hexo": {
    "version": "4.2.0"
  },
  "dependencies": {
    "hexo": "^4.2.0",
    "hexo-abbrlink": "^2.0.5",
    "hexo-generator-category": "^1.0.0",
    "hexo-generator-feed": "^2.2.0",
    "hexo-generator-indexed": "^1.0.1",
    "hexo-generator-search": "^2.4.0",
    "hexo-generator-sitemap": "^2.0.0",
    "hexo-generator-tag": "^1.0.0",
    "hexo-related-popular-posts": "^4.0.0",
    "hexo-renderer-ejs": "^1.0.0",
    "hexo-renderer-marked": "^2.0.0",
    "hexo-renderer-stylus": "^1.1.0",
    "hexo-server": "^1.0.0",
    "image-size": "^0.8.1"
  }
}
