{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "hexo clean && mkdir public && cp -r en public && hexo generate",
    "clean": "hexo clean",
    "deploy": "hexo deploy",
    "server": "rm -rf public && mkdir public && cd ../blog-en && hexo cl && hexo g && cp -r public ../blog/public/en && cd ../blog && hexo s"
  },
  "hexo": {
    "version": "4.2.0"
  },
  "dependencies": {
    "hexo": "^4.2.0",
    "hexo-abbrlink": "^2.0.5",
    "hexo-generator-archive": "^1.0.0",
    "hexo-generator-category": "^1.0.0",
    "hexo-generator-indexed": "^1.0.1",
    "hexo-generator-search": "^2.4.0",
    "hexo-generator-sitemap": "^2.0.0",
    "hexo-generator-tag": "^1.0.0",
    "hexo-related-popular-posts": "^4.0.0",
    "hexo-renderer-ejs": "^1.0.0",
    "hexo-renderer-marked": "^2.0.0",
    "hexo-renderer-stylus": "^1.1.0",
    "hexo-server": "^1.0.0",
    "image-size": "^0.8.1"
  }
}
